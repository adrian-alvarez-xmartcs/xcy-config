import React from 'react';
import useTitle from '../hooks/useTitle/useTitle';

const ConfigPage = () => {
	useTitle("Library")

	return (
		<>
			<h1>HELLO</h1>
		</>
	);
}
		

export default ConfigPage;
